float calc(float a, float b)
{

    // Print the value that we received
    Print("Received: " + a + ", " + b + "\n");
    int c = a * 10;
    a =  c;
    // Print the current system time
    Print("Hello world 90 AngleScript !!");
    
    // Do the calculation and return the value to the application
    return a * b + a + b ;
}
