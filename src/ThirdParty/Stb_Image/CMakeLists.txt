project (stb)
file (GLOB heads *.h)
file( GLOB sources *.cpp)
add_library_beautiful(stb ${heads} ${sources})
install_header_files (DIRECTORY ./ DESTINATION ${DEST_INCLUDE_DIR}/Stb_Image FILES_MATCHING PATTERN *.hpp *.h USE_FILE_SYMLINK BUILD_TREE_ONLY) 