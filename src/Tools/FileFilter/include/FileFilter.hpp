#include <string>
#include<sstream>
#include <iostream>
#include <vector>
#include <map>
using namespace std;

std::vector<std::string> FileFilter(const char * dir, std::vector<std::string> filefilters);