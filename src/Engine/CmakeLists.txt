file(GLOB headers "Core/Engine.h" "Core/Context.h" "Core/Timer.h")
file(GLOB sources "Core/Engine.cpp" "Core/Context.cpp" "Core/Timer.cpp")
include_directories("../../include")									#添加头文件目录
include_directories("../Engine")    	

list(APPEND sources ${headers} ${DIR_Main})

add_library(TmingEngine ${sources}) 
#target_link_libraries(TmingEngine lua)

add_definitions(-DCOMPILING_LIB)
