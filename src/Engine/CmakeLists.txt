file(GLOB_RECURSE headers 
"Core/*.h"
"Log/*.h"
"Core/*.h"
"FileSystem/*.h"
)
file(GLOB_RECURSE sources 
"Core/*.cpp" 
"Log/*.cpp"
"FileSystem/*.cpp" 
"ImGui/*.c"
"ImGui/*.c??"
"3Part/STB/*.cpp"
"../../include/*.cpp"
"Model/model.cpp"
) 
set(CXX_STANDARD 14)

include_directories("../../include")									#添加头文件目录
include_directories("../Engine")    	
include_directories("../Engine/3Part/Box2D")
include_directories("../Engine/3Part/Box2D/Testbed/Framework")    #添加头文件目录

set(libpath ${PROJECT_SOURCE_DIR}/../Library/static)
if(WIN32)
find_library (GLFW glfw3.lib ${libpath})
elseif(APPLE)
find_library (GLFW libglfw3.a ${libpath}) 
endif()

add_library(TmingEngine  ${headers} ${sources})
target_link_libraries(TmingEngine lua)
target_link_libraries(TmingEngine Box2D)
target_link_libraries(TmingEngine ${GLFW})

add_definitions(-DCOMPILING_LIB)

add_subdirectory(3Part/Box2D)

