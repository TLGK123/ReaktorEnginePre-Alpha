file(GLOB headers "Core/Engine.h" "Core/Context.h" "Core/Timer.h" "Log/ILogger.h" "Log/Log.h" "Core/Event.h")
file(GLOB sources "Core/Engine.cpp" "Core/Context.cpp" "Core/Timer.cpp" "Log/Log.cpp")
include_directories("../../include")									#添加头文件目录
include_directories("../Engine")    	

list(APPEND sources ${headers} ${DIR_Main})

add_library(TmingEngine ${sources}) 
target_link_libraries(TmingEngine lua)
target_link_libraries(TmingEngine Box2D)

add_definitions(-DCOMPILING_LIB)

add_subdirectory(3Part/Box2D)
